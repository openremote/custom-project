apply plugin: "java-library"

dependencies {
    api resolveDependency(":container")
    api resolveDependency(":manager")
    api project(":agent")
    api project(":model")
}

task installDist {
    dependsOn jar
}
